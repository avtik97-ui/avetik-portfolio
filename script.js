const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const hero=document.querySelector('.hero');const portrait=document.getElementById('heroPortrait');if(hero&&portrait){let tx=0,ty=0,cx=0,cy=0;hero.addEventListener('mousemove',e=>{const r=hero.getBoundingClientRect();tx=((e.clientX-r.left)/r.width-.5)*2;ty=((e.clientY-r.top)/r.height-.5)*2});hero.addEventListener('mouseleave',()=>{tx=0;ty=0});function loop(){cx+=(tx-cx)*.05;cy+=(ty-cy)*.05;portrait.style.transform=`translate3d(${cx*12}px,${cy*8}px,0)`;requestAnimationFrame(loop)}if(!matchMedia('(prefers-reduced-motion: reduce)').matches)loop()}